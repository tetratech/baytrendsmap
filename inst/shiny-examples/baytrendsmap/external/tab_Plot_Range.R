# Create a map based on user selections
# uiOutput coded in server.R
#~~~~~~~~~~~~~~~~~
#
function(){
  tabPanel("3.a. Plot Range"
           , fluidPage(h2("Plot Range")
          , br()
          #, p(paste0("abc", nrow_df_import(), "xyz"))
            
             
           )##fluidPage~END
  )##tabPanel~END
  
}##FUNCTION~END